{
  "name": "iot-telemetry-lab",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build:apps",

    "build:packages": "npm --workspace packages/contracts run build && npm --workspace packages/device-catalog run build",

    "build:apps": "npm --workspace apps/ingestion-api run build && npm --workspace apps/device-simulator run build && npm --workspace apps/telemetry-worker run build && npm --workspace apps/anomaly-worker run build && npm --workspace apps/alert-worker run build"
  }
}