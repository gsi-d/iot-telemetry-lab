{
  "name": "iot-telemetry-lab",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:contracts": "npm run build --workspace=@iot/contracts",

    "build:catalog": "npm run build:contracts && npm run build --workspace=@iot/device-catalog",

    "build:shared": "npm run build:contracts && npm run build --workspace=@iot/device-catalog",

    "dev:api": "npm run build:shared && npm run dev --workspace=@iot/ingestion-api",

    "dev:simulator": "npm run build:shared && npm run dev --workspace=@iot/device-simulator",

    "dev:worker": "npm run build:shared && npm run dev --workspace=@iot/telemetry-worker",

    "dev:anomaly": "npm run build:shared && npm run dev --workspace=@iot/anomaly-worker",

    "dev:alerts": "npm run build:shared && npm run dev --workspace=@iot/alert-worker",

    "build": "npm run build --workspaces"
  }
}